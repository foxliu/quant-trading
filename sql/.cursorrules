# sql/.cursorrules

## 本目录核心职责
- 存放所有需要人工执行的 SQL 脚本。
- 统一管理数据库初始化、修复、补数、回滚类 SQL。

## 必须遵守
- 手动 SQL 只能放在 `sql/` 目录。
- 脚本文件必须按顺序编号：`NNN_描述.sql`。
- 每个脚本必须在文件头说明用途、风险、执行前提。
- 优先幂等写法（`IF NOT EXISTS` / `ON CONFLICT`）。

## 禁止
- 禁止把手动 SQL 散落到 `cmd/`、`internal/`、`docs/` 等目录。
- 禁止提交未说明用途与影响范围的 SQL。
- 禁止在脚本中硬编码真实生产敏感信息。
