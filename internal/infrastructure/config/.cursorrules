# internal/infrastructure/config/.cursorrules

## 本目录核心职责
- 负责配置结构体定义、加载与解析
- 提供统一配置入口给 cmd 层使用

## 必须遵守
- 配置字段命名与 yaml 键保持一一对应
- 加载错误必须返回明确错误信息
- 禁止在配置层注入业务逻辑

## 禁止
- 禁止依赖 application 业务对象
- 禁止直接访问数据库或交易通道
