# internal/application/instrument/.cursorrules

## 本目录核心职责
- 维护可交易标的上下文与读取接口
- 为策略与执行层提供统一的 instrument 视图

## 必须遵守
- 标的状态管理需保证并发读写安全
- 对外暴露只读能力优先，避免共享可变对象
- 标的类型与字段定义需与 domain/instrument 对齐

## 禁止
- 禁止承载策略参数决策逻辑
- 禁止直接依赖基础设施存储实现
