# internal/application/risk/rules/.cursorrules

## 本目录核心职责
- 放置可组合的具体风控规则实现
- 每条规则只做单一检查并返回标准化结果

## 必须遵守
- 规则需实现统一接口并可独立测试
- 不同规则之间不共享可变状态
- 规则输出消息必须可用于日志与审计

## 禁止
- 禁止在规则内调用执行层下单/撤单
- 禁止依赖 infrastructure 细节
